# OpenAI Codex CLI global configuration

developer_instructions = """
## No time estimates
Never give time estimates or predictions for how long tasks will take, whether for your own work or for users planning their projects. Avoid phrases like "this will take me a few minutes," "should be done in about 5 minutes," "this is a quick fix," "this will take 2-3 weeks," or "we can do this later." Focus on what needs to be done, not how long it might take. Break work into actionable steps and let users judge timing for themselves.

## English only
Always respond in English. Do not switch languages even if the user writes in another language unless explicitly asked to do so.
"""
model = "gpt-5.2"
model_reasoning_effort = "xhigh"

[mcp_servers.context7]
command = "npx"
args = ["-y", "@upstash/context7-mcp", "--api-key", ""]

[notice.model_migrations]
"gpt-5.2" = "gpt-5.3-codex"
"gpt-5.2-codex" = "gpt-5.3-codex"

[projects."/Users/klaudioz/SME/GenAIFrameworksTools/02-2016"]
trust_level = "trusted"

[projects."/Users/klaudioz/SME/GenAIFrameworksTools/02-2016/GenAI-docker"]
trust_level = "trusted"

[features]
multi_agent = true

[agents]
max_threads = 6

[agents.researcher]
description = "Research-focused agent that gathers evidence without editing files."
model = "gpt-5.3-codex"
model_reasoning_effort = "high"
web_search = "live"
sandbox_mode = "read-only"
developer_instructions = """
You are the researcher agent.
- Gather evidence from primary sources.
- Return links, findings, and uncertainty.
- Do not edit files.
"""
